name: scribe
description: The CLI a project built on scribe uses, from init to code generation.
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:
  change_case: ^2.2.0
  cli_util: ^0.5.1
  crypto: ^3.0.7
  dart_console: ^1.2.0
  interact: ^2.2.0
  path: ^1.9.1
  yaml: ^3.1.3
  string_validator: ^1.2.0
  tint: ^2.0.1
  fiber_shell: ^1.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
