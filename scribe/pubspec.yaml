name: scribe
description: The CLI a project built on scribe uses, from init to code generation.
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:
  change_case: ^2.2.0
  crypto: ^3.0.7
  path: ^1.9.1
  yaml: ^3.1.3
  fiber_shell: ^1.0.0
  interact: ^2.2.0
  file: ^7.0.0
  args: ^2.6.0
  meta: ^1.16.0
  dage: ^1.0.10
  collection: ^1.19.1
  cryptography: ^2.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
