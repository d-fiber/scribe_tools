name: sdk
description: The CLI the scribe framework itself uses, internal to framework development.
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:
  args: ^2.7.0
  change_case: ^2.2.0
  cli_util: ^0.5.1
  crypto: ^3.0.7
  interact: ^2.2.0
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
