outputs:
  host: "${POSTGRES_HOST}"
  port: "${POSTGRES_PORT:-5432}"
  database: "${POSTGRES_DATABASE:-postgres}"
  user: "${POSTGRES_USER}"
  password: "${POSTGRES_PASSWORD}"
  url: "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT:-5432}/${POSTGRES_DATABASE:-postgres}"
