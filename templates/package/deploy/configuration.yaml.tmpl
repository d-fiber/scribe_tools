# What a project may tune about this package, and what infrastructure it needs.
# Both blocks are optional; leave the file out entirely if neither applies.
#
# settings:
#   max_items:
#     doc: "The most items the API returns in one page."
#     type: integer
#     default: 50
#
# requires:
#   - name: index
#     type: opensearch
#
#   - name: postgres
#     type: postgres
#     capabilities: [pg_cron]
#     # A capability names an extension or a right the placed recipe must
#     # already promise. Only add one when this package's own SQL needs it.
