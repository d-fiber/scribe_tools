/**
 * What "{{name}}" hands whoever mounts it.
 *
 * Everything it is made of lives in `src/`, and this file is the only way in. A
 * package may also export `wires`, `starts` and `stops` from here, which are the three moments
 * the host runs it at.
 */

export {};
