services:
  nats:
    cpu_shares: 1024
    memswap_limit: "{{nats_mem_limit}}"
    deploy:
      resources:
        limits:
          memory: "{{nats_mem_limit}}"{{nats_cpu_ceiling}}
          pids: 512
        reservations:
          memory: "{{nats_mem_res}}"
