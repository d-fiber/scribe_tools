name: "{{app_name_snake}}"

services:
  worker:
    cpu_shares: 1024
    deploy:
      resources:
        limits:
          memory: "{{worker_mem_limit}}"{{worker_cpu_ceiling}}
          pids: 512
        reservations:
          memory: "{{worker_mem_res}}"
