name: "{{app_name_snake}}"

services:
  caddy:
    cpu_shares: 2048
    memswap_limit: "{{caddy_mem_limit}}"
    deploy:
      resources:
        limits:
          memory: "{{caddy_mem_limit}}"{{caddy_cpu_ceiling}}
          pids: 512
        reservations:
          memory: "{{caddy_mem_res}}"
